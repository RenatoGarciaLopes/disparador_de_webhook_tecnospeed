const path = require("node:path");
require("ts-node/register");
require("dotenv").config();

module.exports = {
  config: path.resolve("sequalize", "config.ts"),
  "models-path": path.resolve("sequalize", "models"),
  "seeders-path": path.resolve("sequalize", "seeders"),
  "migrations-path": path.resolve("sequalize", "migrations"),
};
